name: sample demo
conda_env: conda.yaml

entry_point:
    main:
        parameters:
            params1: {type: int, default: 1}
            params2: {type: int, default: 2}
        command: "python demo.py -p1 {params1} -p2 {params2}"
        